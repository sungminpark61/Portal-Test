using UnityEngine;
using UnityEngine.Rendering;
using UnityEngine.Rendering.Universal;

// 임시 수정된 스크립트 (오류가 있지만 진단용으로 잠시 사용)
public class PortalStencilToggler : MonoBehaviour
{
    public string targetFeatureName = "Stencil Target";
    private RenderObjects m_StencilTargetFeature;

    void Start()
    {
        // 이 스크립트의 내용은 잠시 무시합니다.
    }

    void Update()
    {
        // 이 스크립트의 내용은 잠시 무시합니다.
    }
}